{
  "name": "weather-for-grown-ups",
  "version": "0.1.0",
  "description": "Agent-native access to NOAA GFS data.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/janhelcl/weather-for-grown-ups.git"
  },
  "bugs": {
    "url": "https://github.com/janhelcl/weather-for-grown-ups/issues"
  },
  "homepage": "https://github.com/janhelcl/weather-for-grown-ups#readme",
  "keywords": [
    "gfs",
    "noaa",
    "weather",
    "meteorology",
    "grib2",
    "mcp",
    "model-context-protocol"
  ],
  "files": [
    "dist",
    "*.md",
    "LICENSE"
  ],
  "engines": { "node": ">=20" },
  "bin": {
    "wfg": "./dist/cli.js",
    "wfg-mcp": "./dist/mcp-cli.js",
    "wfg-mcp-http": "./dist/mcp-http.js"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "mcp": "tsx src/mcp.ts",
    "mcp:http": "tsx src/mcp-http.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:smoke": "node dist/cli.js --help && node dist/cli.js catalog --help && node dist/cli.js catalog --search cloud --sections fields --classification raw --limit 3 --json && node dist/cli.js latest --help && node dist/cli.js profile --help && node dist/cli.js layer --help && node dist/cli.js profile-diagnostics --help && node dist/cli.js parcel --help && node dist/cli.js diagnostic-timeseries --help && node dist/cli.js points --help && node dist/cli.js transect --help && node dist/cli.js timeseries --help && node dist/cli.js points-timeseries --help && node dist/cli.js compare-runs --help && node dist/cli.js area --help",
    "test:live": "tsx scripts/live-smoke.ts",
    "test:live:s3": "tsx scripts/live-smoke-s3.ts",
    "test:live:area": "tsx scripts/live-smoke-area.ts",
    "test:live:all": "npm run test:live:s3 && npm run test:live:area",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.scripts.json --noEmit",
    "pack:check": "npm pack --dry-run",
    "prepack": "npm run build",
    "prepublishOnly": "npm run typecheck && npm test && npm run build && npm run test:smoke"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "commander": "^14.0.0",
    "execa": "^9.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^24.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
